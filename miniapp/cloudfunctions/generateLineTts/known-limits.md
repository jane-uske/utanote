# Known limits

- This is spoken TTS, not singing synthesis.
- The frontend still calls `generateLineTts`; `ensureTtsAsset` is an optional alias.
- Advanced token `audioText` rules can be improved later for richer grammar chunks.
- Build and VOICEVOX runtime tests should be run locally before merging.
